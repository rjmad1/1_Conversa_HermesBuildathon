# Conversa — Enterprise Cognitive Platform Architecture

---
### 📋 Document Metadata
- **Purpose**: Architectural specification defining platform layers, C4 context, component boundaries, sequence flows, and 3-hash publishing verification.
- **Audience**: Enterprise architects, security leads, SREs, and platform engineers.
- **Last Generated**: 2026-07-20T06:54:00+05:30
- **Confidence Level**: Verified (Directly grounded in codebase implementation and passing test suites).
- **Evidence Used**: `src/modules/*` codebase, `convex/schema.ts`, Next.js UI shell, and 42 vitest test suites.
- **Cross References**: See [PROJECT.md](file:///c:/Users/rajaj/Projects/1_Conversa/docs/PROJECT.md), [MODULES.md](file:///c:/Users/rajaj/Projects/1_Conversa/docs/MODULES.md), [IMPLEMENTATION_STATUS.md](file:///c:/Users/rajaj/Projects/1_Conversa/docs/IMPLEMENTATION_STATUS.md).
---

## 1. Enterprise C4 Context

```mermaid
graph TD
    User([Enterprise User / Leadership]) -- Ingest Audio / Transcripts --> UI[Next.js 16 Spatial UI Shell]
    UI -- Invokes Reactive API / Functions --> Convex[(Convex Serverless Engine)]
    UI -- Executes Pipeline --> PlatformBus[Platform Event Bus & Hono API]
    
    subgraph Enterprise Cognitive Platform
        PlatformBus --> P1[Phase 1: Cognitive Meeting Pipeline]
        P1 --> P2[Phase 2: Cognitive Collaboration Engine]
        P2 --> P3[Phase 3: Knowledge Publishing Layer]
        P3 --> P4[Phase 4: Living Workspace Layer]
    end

    P1 -- Provider Fallback --> PrimaryLLM[(OpenAI)]
    P1 -- Failover LLM --> SecondaryLLM[(Anthropic)]
    P1 -- Web Grounding --> LinkupAPI[(Linkup Grounding Search)]
    P3 -- Output Artifacts --> Export[Markdown / JSON / HTML / PlainText]
```

---

## 2. Platform Layer Component Architecture

```mermaid
graph LR
    subgraph Phase 1: Meeting Pipeline
        CR[Capability Router] --> FO[Provider Failover]
        FO --> AC[Managed Agency Crew]
    end

    subgraph Phase 2: Collaboration Engine
        EB[Evidence Blackboard] --> DC[Debate Coordinator]
        DC --> VE[Validation Engine]
        VE --> CG[Consensus Generator]
    end

    subgraph Phase 3: Knowledge Publishing
        SPB[Semantic Publication Bus] --> Pubs[Publishers Catalog]
        Pubs --> Rends[Multi-Format Renderers]
        Rends --> Hash3[3-Hash Verification Manifest]
    end

    subgraph Phase 4: Living Workspace
        LKG[Living Knowledge Graph] --> TL[Workspace Timeline]
        TL --> HE[Health Engine]
        HE --> RE[Recommendation Engine]
    end

    AC --> EB
    CG --> SPB
    SPB --> LKG
```

---

## 3. End-to-End Enterprise Data Flow

### 3.1 Audio Ingestion to Cryptographic Knowledge Publication
```mermaid
sequenceDiagram
    autonumber
    actor User as Enterprise Stakeholder
    participant Pipeline as Phase 1: Cognitive Pipeline
    participant Blackboard as Phase 2: Evidence Blackboard
    participant Consensus as Phase 2: Consensus Generator
    participant PubBus as Phase 3: Semantic Publication Bus
    participant Graph as Phase 4: Living Knowledge Graph

    User->>Pipeline: Submit Meeting Audio / Transcript
    Pipeline->>Pipeline: Capability Router & Multi-Agent Extraction
    Pipeline->>Blackboard: Append Raw Evidence Packages
    Blackboard->>Consensus: Trigger Debate Coordinator & Validation
    Consensus->>Consensus: Calculate Composite Confidence & Sanitize PII
    Consensus->>PubBus: Emit ValidatedKnowledgePackage
    PubBus->>PubBus: Execute Executive / Engineering / Action Publishers
    PubBus->>PubBus: Compute 3-Hash Lineage (semantic, content, provenance)
    PubBus->>Graph: Ingest Entities & Edges into Living Knowledge Graph
    PubBus-->>User: Deliver Deterministic Signed Published Artifacts
```

---

## 4. Cryptographic 3-Hash Lineage Verification Model

Every published artifact generated by the Enterprise Knowledge Publishing Layer contains an immutable 3-hash lineage manifest:

```json
{
  "publicationId": "pub_exec_001",
  "sourcePackageId": "vkp_meeting_99",
  "semanticHash": "a4f8e... (SHA-256 of Canonical Semantic Model)",
  "contentHash": "7b12c... (SHA-256 of Rendered Output Artifact)",
  "provenanceHash": "9e33f... (SHA-256 of Provenance & Evidence Chain)"
}
```

* **`semanticHash`**: Guarantees canonical semantic equivalence across different audience views.
* **`contentHash`**: Ensures rendered output integrity (Markdown / HTML / JSON / Text).
* **`provenanceHash`**: Cryptographically anchors facts back to raw transcript line numbers and speaker evidence.

---

## 5. Security & Isolation Boundaries

1. **Multi-Tenancy Scoping**: Strict boundary checking enforced on every request (`tenantId`, `workspaceId`).
2. **Data Residency Guardrails**: Package policy checks enforcing data residency rules (`US`, `EU`, `India`, `Global`, `CustomerManaged`, `AirGapped`).
3. **Log Redaction**: Automatic recursive redaction of sensitive credentials, tokens, and PII in system telemetry.
