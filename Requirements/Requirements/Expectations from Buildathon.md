You will need to do THESE aspects to be part of the event:

1. Post that you are building this, right now: the announcement post is your first impressions and your first signups.
2. Put a waitlist page live before you build anything: one headline, one email field, hosted on Cloudflare Pages.
3. Stand up the crew skeleton: an intel chief agent managing per-competitor researcher subagents plus an analyst and QA, on one competitor first.
4. Connect the real surfaces early: the battlecard dashboard on Cloudflare Pages and the daily digest wired into a real Slack channel via the gateway.
5. Build the run log mentors will read: each researcher's findings with Linkup source links, the analyst's diff, and the QA source check per sweep.
6. Take the day-one snapshot of every competitor's pricing pages, changelogs, and news into Convex memory, because a diff needs a yesterday.
7. Run one full end-to-end sweep: researchers fan out, the analyst diffs against the snapshot, QA checks sources, and the digest lands in Slack.
8. Overflow with repeated sweeps: wire the 8am cron and let it fire for real across event days so the diff history is genuine.
9. Rehearse the demo: show yesterday's snapshot, trigger the cron live, narrate the fan-out and diff in the run log, and close as the digest lands in Slack and a rep replies.
